services:
  postgres:
    image: postgres:18-alpine
    container_name: gameproject-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-gameproject}
      POSTGRES_USER: ${POSTGRES_USER:-gameproject}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-gameproject_dev_password}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - gameproject-postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s

volumes:
  gameproject-postgres-data:
